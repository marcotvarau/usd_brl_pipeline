# Python 3.13 Compatible Requirements
# Using only pre-compiled wheels to avoid build issues

# Core dependencies - pre-compiled versions
numpy>=1.26.0,<2.0.0  # Use latest 1.x with wheels
pandas>=2.1.0,<2.3.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
sqlalchemy>=2.0.0,<2.1.0

# Data collection
requests>=2.31.0
yfinance>=0.2.30
fredapi>=0.5.0
python-bcb>=0.1.9
beautifulsoup4>=4.12.0
lxml>=4.9.0  # Should have wheels for Python 3.13

# Data processing - only packages with Python 3.13 wheels
scipy>=1.11.0  # Skip for now if problematic
scikit-learn>=1.3.0  # Skip for now if problematic
# statsmodels - skip for now, use alternative
ta>=0.10.2

# Validation and quality - simplified
# great-expectations - skip for now, complex dependencies
pandera>=0.17.0
pydantic>=2.5.0

# Caching and persistence
redis>=5.0.0
pyarrow>=14.0.0  # Should have wheels
# fastparquet - might be problematic
openpyxl>=3.1.0
# tables - skip HDF5 for now

# Scheduling and async
schedule>=1.2.0
# apscheduler - might be problematic
aiohttp>=3.9.0

# Monitoring and logging
loguru>=0.7.0
python-json-logger>=2.0.0
# prometheus-client - might be problematic

# Reliability patterns
tenacity>=8.2.0
# pybreaker - might be problematic
backoff>=2.2.0

# News sentiment analysis - simplified
textblob>=0.17.0
# vaderSentiment>=3.3.0
# newspaper3k - might be problematic
# googletrans - might be problematic

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
faker>=20.0.0

# Development tools
black>=23.12.0
# flake8>=6.1.0
# mypy>=1.7.0
# pre-commit>=3.6.0
ipykernel>=6.27.0
jupyter>=1.0.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Database drivers
# psycopg2-binary - might not have Python 3.13 wheels yet
pymongo>=4.6.0

# API documentation
# pydantic-yaml>=1.2.0
marshmallow>=3.20.0

# Alternative packages for missing functionality
# Use these instead of problematic packages:
# For statistics: use scipy.stats instead of statsmodels
# For data validation: use pydantic instead of great-expectations
# For monitoring: use built-in logging instead of prometheus-client